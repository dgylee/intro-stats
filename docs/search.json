[
  {
    "objectID": "examples/ex1-pepsi-v-coke.html",
    "href": "examples/ex1-pepsi-v-coke.html",
    "title": "Non-random sampling: Pepsi vs. Coke",
    "section": "",
    "text": "Below are the details of the setup but hidden from observers during the simulation presentation.\n\n\nDetails\n\n\n“Execution” steps:\n\nClass is polled about preference between Pepsi and Coke using an anonymous survey (e.g. Slido) but results are not known\nExample: 18 prefer Pepsi vs 40 prefer Coke\nIn order, the input is provided in the code below and the results are simulated r times\nIMPORTANT: This example is setup to create a biased result where Pepsi is preferred over Coke\n\nThe code looks like this:\n\nr &lt;- 10\ndo.call(rbind, lapply(1:r, function(x) RCT(18, 40)))\n\nand required two inputs:\n\npepsi = 18\ncoke = 40\n\nwhile containing two default inputs (lower and upper limit of the proportion of the population size used to generate the sample)\n\nll = 0.20\nul = 0.70\n\nalong with an optional random seed (i.e. set.seed) so a demonstration can be reproduced exactly.\nExample output:\n\n        p_pepsi    p_coke   n       \n [1,] 0.7222222 0.2777778  18 184450\n [2,] 0.6923077 0.3076923  13 230120\n [3,] 0.7058824 0.2941176  17  64978\n [4,] 0.6923077 0.3076923  13  60157\n [5,] 0.7333333 0.2666667  15   8160\n [6,] 0.6923077 0.3076923  13  60487\n [7,] 0.7058824 0.2941176  17   3341\n [8,] 0.7142857 0.2857143  14  30196\n [9,] 0.7142857 0.2857143  14  22713\n[10,] 0.7142857 0.2857143  14 230149\n\nOutput includes proportion that prefer Pepsi, coke, sample size, and number of (repeated, failed) trials to get this “result”. Sometimes, it may take several attempts of “failed” samples to get the result and others, only 1 time, which is actually great for teaching purposes, highlighting that (legitimately) the SRS can fail.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nFor those interested, I have included the actual code for the function RCT. This is very much outside of the context of this course; however, as (somewhat) described above, this code will take the count for pepsi and coke (in that order) and then determine who is the “favourite”, then it will purposefully sample (and re-sample while keeping track of the number of times executed) until the opposite “result” is observed. The sample size varies and is defined by the ll and ul (the lower and upper limit for the sample size based on the total population).\n\n\nR Code\n\n\n  RCT &lt;- function(pepsi,coke,ll=0.2,ul=0.7){\n    options(scipen = 999)\n    popn  &lt;- rep(c('p','c'),times=c(pepsi,coke))\n    ppep  &lt;- pepsi/(pepsi+coke)\n    pcok  &lt;- 1-ppep\n    iter  &lt;- 10000\n    nst   &lt;- round((pepsi+coke)*ll)\n    if(nst %% 2 == 0) nst &lt;- nst + 1\n    n     &lt;- seq(nst,round((pepsi+coke)*ul,0))\n    n     &lt;- sample(n,length(n))\n    count &lt;- 0\n    exit  &lt;- FALSE\n    exiti &lt;- FALSE\n    exitj &lt;- FALSE\n    maxed &lt;- FALSE\n    while(!exit){\n      for(i in n){\n        for(j in 1:iter){\n          count &lt;- count + 1\n          samp  &lt;- sample(popn,i)\n          if(pepsi &gt; coke){\n            phat  &lt;- mean(samp=='p')\n            if(phat &gt; ppep){\n              exitj &lt;- !exitj\n              # cat('flag1\\n')\n            }\n          } else {\n            phat  &lt;- mean(samp=='p')\n            if(phat &gt; pcok){\n              exitj &lt;- !exitj\n              # cat(phat,'\\n')\n              # cat(pcok,'\\n')\n              # cat('flag2\\n')\n            }\n          }\n          if(exitj){\n            exiti &lt;- !exiti\n            # cat('flag3\\n')\n            break\n          } \n        }\n        if(exiti){\n          # cat('flag4\\n')\n          break\n        }\n      }\n      if(exiti) exit &lt;- !exit\n      if(i==n[length(n)] & j==iter){\n        exit  &lt;- !exit\n        maxed &lt;- !maxed\n      }\n    }\n    if(maxed){\n      out &lt;- c(NA,NA,i,count)\n    } else {\n      out &lt;- c(phat,1-phat,i,count)\n    }\n    names(out) &lt;- c('p_pepsi','p_coke','  n',' ')\n    return(out)\n  }"
  },
  {
    "objectID": "about.html",
    "href": "about.html",
    "title": "About",
    "section": "",
    "text": "The examples posted on this site are for introductory statistics used while teaching. They are not comprehensive (nor 100% error free), but serve the purpose of either showcasing important examples (e.g. results of a “not-so-random” test) or interactive learning (e.g. heatmaps of random sampling) that students can edit live through their web browser."
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Examples for Intoductory Statistics",
    "section": "",
    "text": "The following examples are used throughout my introductory statistics course(s), albeit in no particular order, and are free to reference but are not free from errors (i.e. for demonstration purposes-only).\n\nExample 1: Non-random sampling: Pepsi vs. Coke\nExample 2: Random sampling and visualizing via heatmaps"
  },
  {
    "objectID": "examples/ex2-random-sampling.html",
    "href": "examples/ex2-random-sampling.html",
    "title": "Random sampling (visualized via heatmaps)",
    "section": "",
    "text": "Randomness (sampling and otherwise)\nRandomness will have important ramifications in statistics, both in terms of representativeness and reducing bias, although most discussions will be limited and involve a lot of handwaving. Nonetheless, when things are done “at random”, then we should not (for example) be able to predict with 100% certainty an outcome.\nPer the example that we did (or will discuss), when we take a random sample of 10 numbers from 20, then we should not be able to predict what 10 numbers will be part of the sample. In R, we can use the following code to generate a sample of 10 numbers from 1 through 20:\n\n\n\n\n\n\n\n\nImportantly, if you run the code your output will be different from mine, the person next to you, and anyone else who runs the same code. However, if you use the command set.seed(1) before running the sample function like so,\n\n# Re-running the code so we get the same value\n\nset.seed(1)\nsample(1:20,10)\n\nwe get the same results of: 4, 7, 1, 2, 13, 19, 11, 17, 14, 3. We don’t need to get into the specifics, but setting the seed to a specific number creates reproducible results (i.e. no set.seed, the results will change but with it, you’ll get the same results no matter how many times you hit the ▶ Run Code button).\n\n\n\n\n\n\n\n\n\n\nVisualization (heat map)\nIf you have repeatedly hit the ▶ Run Code button (sans set.seed), then you’ll notice that the 10 numbers have changed repeatedly. Importantly, if we are truly randomly sampling 10 numbers from a set of 20, then if we visualized which of the 10 numbers (from our set of 20) are selected over and over again, there should be no obvious patterns. To visualize this, a heatmap (a data visualization that converts a matrix of data into colors) is a great option - you can learn more in our DSCI204 class.\nSimilar to the code above, we can play around with the following chunk of code that uses a custom function created for this purpose random_heatmap, where the only inputs are (1) how many trials (n_trials) and (2) the values (values) we are selecting from. By default, the inputs are 10 trials and selecting from the numbers 1:20 but feel free to modify these values to see how the heatmap changes. For example, since the code will always split the data into two groups, if you swap the values to (for example) 1:30, then 15 values will be highlighted in the map.\n\n\n\n\n\n\n\n\nIf you play around with this, you’ll (hopefully) notice that there is no distinct pattern (i.e. it is random) and (if you want to), you can add in an option to print the frequency table of the values you are sampling from to see their frequency across the 10 trials.\n\nrandom_heatmap(10, 1:20, table=TRUE)\n\nIt is worth noting that:\n\nFor more than 100 trials, the graph gets a bit weird and so the heatmap won’t print out and instead, you’ll the frequency table.\nFor each of the values (1:20), they “should” be observed half of the time, but that isn’t always the case; however, if you look at the frequency, you’ll (A) see some are (for example) observed more than 5 times while some are observed less than 5 times but (on average), the number of times should be around 5, and (B) if you increase the number of trials to 100, you’ll see a similar variability but they (again) hover somewhere around half (or 50 times)."
  }
]